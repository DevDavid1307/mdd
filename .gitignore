node_modules
.why-changed/

# Local symlink into .agents/skills/, absolute path — machine-specific.
# Scoped to this one entry so upstream can still track other .claude/ files.
.claude/skills/why-changed
.why-changed/
